const SectionHeading = ({ heading }) => {
  return (
    <div className="text-center text-[30px] text-themeColor font-bold">
      <span className="border-b-2 p-2 border-themeColor">{heading}</span>
    </div>
  );
};

export default SectionHeading;
2;
